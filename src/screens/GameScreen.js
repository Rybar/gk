class GameScreen extends Container {
    constructor(game, controls) {
        super();
        //init code
    }

    update(dt,t) {
        super.update(dt,t);
        //game screen update logic
    }

    
}